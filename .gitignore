# macOS
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS artefacts
Thumbs.db
desktop.ini

# Logs (local development artefacts)
*.log

# Claude Code – machine-/user-local permission allowlist
# (settings.json stays versioned for project-wide defaults)
.claude/settings.local.json

# Maintainer-local working state (CCPR dogfooding its own conventions).
# The repo ships docs/ as framework content; these are session artefacts, not
# distribution. Keeping them untracked also keeps tenant identifiers out of
# shipped artefacts (Constitution, Inviolable "No personal or tenant data").
# Decision drafts are extracted into a de-personalised ADR under docs/adr/.
docs/HANDOVER.md
docs/.handover-archive/
docs/decisions/

# Project working state, not framework content. install.sh copies docs/ wholesale
# into ~/.claude/docs/, so anything here reaches every installation: 17 work items
# about CCPR's own defects and ~146 KB of persona review notes would arrive looking
# like the user's own state. Framework docs that explain the design (adr/,
# CONSTITUTION.md, PROJECT_PHASES.md, the flat Tier-1 memory files) stay tracked.
docs/workitems/
docs/memory/*/
