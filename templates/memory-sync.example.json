{
  "_comment": "Template for scripts/memory-sync.sh. Copy to ~/.claude/memory-sync.json, fill in your shared org-tier repo, and add ~/.claude/memory-sync.json + ~/.claude/.memory-sync/ to your sync exclusions (never distribute this filled copy). See CLAUDE.md, 'Sharing instincts/memory across a team (org tier)'.",
  "repo": "<org>/<repo>",
  "repoUrl": "https://<your-git-host>/<org>/<repo>.git",
  "apiBase": "https://<your-git-host>/api/v1",
  "tokenFile": "~/.<host>-token",
  "clonePath": "~/.claude/.memory-sync/<repo>",
  "namespace": "XX",
  "gate": {
    "_comment": "Shared by scripts/memory-sync.sh (promote path) and scripts/artifact-gate.sh (shipped-artifact sweep). ipAllowlist is a regex of IPv4 prefixes that are NOT reported, e.g. an internal VPN net. denyNames is the list of tenant and project names that must never appear in a shipped artifact or in promoted memory; it is matched case-insensitively as a literal substring, and the names themselves are never echoed into gate output. THE CRITERION IS NAMES THAT DO NOT BELONG IN THE SHARED REPOSITORY -- NOT NAMES THAT BELONG TO YOU. Your own organisation or namespace name must NOT be listed: it is the namespace the shared repository lives in, so it is present by construction in repo, repoUrl and every overlay.* path, and listing it refuses every promote onto every existing file -- a check that always says the same thing stops being read, and then nothing gets scanned at all. See Manual/system/discipline-gate.md, 'What belongs on the list -- and what must not'. Keep this file out of every repository -- putting the names into a tracked file would leak exactly what the check protects. A CI job that cannot read this file can pass the same list via the CCPR_GATE_DENY_NAMES environment variable (newline- or comma-separated) from its secret store.",
    "ipAllowlist": "",
    "denyNames": ["<tenant-or-client-name>", "<internal-project-codename>"]
  },
  "conformance": {
    "_comment": "Read by scripts/conformance-run.sh (docs/adr/ADR-0010-conformance-runs-against-consumers.md). consumers is a list of real projects this operator maintains, addressed by LOCAL FILESYSTEM PATH ONLY -- nothing is ever fetched over a network. Each consumer's id is what reports show; the path never appears in a report unless --show-paths is given. optional (default false) means a missing path is reported as not-covered instead of failing the run -- useful for a consumer that lives on a machine this config is also used from, but is not always checked out. pins are concrete, dated, per-consumer expectations (class C3) -- each MUST carry a non-empty why, printed next to a violation, explaining why this consumer fact is CCPR behaviour rather than consumer content. A pin is exactly one of expectFinding (a substring, or a POSIX ERE when regex: true, that must appear on at least minCount report lines -- default 1) or expectField (one of exit, errors, warnings, info, filesScanned, compared against value -- filesScanned is a floor (>=), every other field is exact equality). Deliberately never a commit SHA, and never a fact about the CONSUMER's own working state either -- a pin's subject must be something CCPR controls, not something that changes the moment that consumer commits. Keep this file out of every repository, same reasoning as gate.denyNames above: a consumer's path is frequently a home directory.",
    "consumers": [
      { "id": "consumer-a", "path": "~/path/to/a-project" },
      { "id": "consumer-b", "path": "~/path/to/b-project", "optional": true }
    ],
    "pins": [
      {
        "consumer": "consumer-a",
        "check": "phase-docs-lint",
        "expectFinding": "holds only a placeholder",
        "why": "this consumer's own reserved-but-empty module directory (a .gitkeep only) must keep reporting as such; if this stops firing, either the module got built (fine) or phase-docs-lint's covers: emptiness check regressed (not fine)"
      },
      {
        "consumer": "consumer-a",
        "check": "memory-lint",
        "expectField": "errors",
        "value": 0,
        "why": "this consumer's docs/memory/ is a frozen fixture and must stay clean against memory-lint"
      }
    ]
  },
  "overlay": {
    "instinctsFileInRepo": "instincts/shared.md",
    "instinctsTarget": "~/.claude/instincts/shared.md",
    "instinctsIndex": "~/.claude/instincts.md",
    "indexBlockTitle": "Shared (org)",
    "personaTopicName": "shared.md",
    "memoryNsDir": "~/.claude/memory/shared",
    "memoryIndexPointer": ""
  }
}
