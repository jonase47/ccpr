---
id: WI-0003
title: Enforce the HANDOVER size cap automatically
status: In Review
type: task
tags: [hygiene, package-a]
created: 2026-08-18
---

The 5 KB / 150-line cap is documented in the template header and enforced by /cleanup, but nothing triggers it. doc-volume-check.sh does not help: its thresholds are 25/40/50 KB, five times the cap. hooks/ exists and settings.json already wires SessionStart, SessionEnd and PreToolUse. Field observation 18.08.2026: one full skill run took this repo's own HANDOVER from 3785 to 4806 bytes (94 percent of cap) and only stayed under by replacing blocks rather than appending; a later run needed a manual compaction pass. Follow the two-tier check-location pattern settled with ADR-0009.

## Acceptance Criteria

## Result
- 52e5103
## Comments
- Implemented, hardened and reviewed across four rounds; final delta approved as mergeable with no critical or important findings. Three defects fixed and independently re-verified: malformed-payload escape in the write gate, rounding-decided severity at the cap boundary (a 5097-byte file was announced as over its 5120-byte cap), and the same rounding error live at the approaching threshold. Level now decided on exact integer ratios, reported percentage stays rounded. 437 tests green. Follow-ups: WI-0009 (three of four gate tools untested), WI-0010 (parser tolerances unexercised), WI-0011 (cap guards and the rounded-percentage log field). Operational note: the feature is inert on an installation until install.sh --update runs, since the installed hook predates it; /cleanup falls back correctly in the meantime.
