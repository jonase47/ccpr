---
id: WI-0002
title: HANDOVER template: add an append-only TODO inbox
status: In Review
type: task
tags: [hygiene, package-a]
created: 2026-08-18
---

HANDOVER_TEMPLATE.md has exactly one collection point, the 'Open Decisions' table. Repo-wide grep for an inbox mechanism: 0 hits. A decision table is for 'the PO must resolve this', not for 'I noticed something while working' -- without a second slot the finding either interrupts the task or is lost. Shape from GitHub issue #10: append-only for working agents, cleaned up by a supervising role, which maps onto the existing split (agents append, /cleanup and project-guide tidy). Touches templates/HANDOVER_TEMPLATE.md, commands/cleanup.md, possibly agents/project-guide.md. Additive -- no ADR threshold.

## Acceptance Criteria

## Result
- 365ca98
## Comments
- Implemented and reviewed. Three blocking findings fixed and independently re-verified: unescaped marker (grep vs grep -E now agree at 2 on a probe with a literal pipe in the finding), parser hijack (A/B with two HANDOVERs differing in one inbox line now yields identical allowed_commands), and the producer/consumer format mismatch (p5-polish emits the marker format, POL-ID in the ref field). Fresh template reports 0 entries. bootstrap.sh exits 0 with correct Next Steps. Follow-ups opened as WI-0006 (Open Points name collision across 93 command files) and WI-0007 (unguarded HANDOVER reset in /release-baseline). Left In Review, not Done: the fix round did not get its own review pass.
